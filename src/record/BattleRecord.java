package record;

import entity.Player;
import entity.EntityStatus;
import entity.Foe;
import entity.Entity;

public class BattleRecord extends Record {

	public BattleRecord(Entity p, Entity f) {
		super(p, f);
	}
}
